    codemirrorLanguage.LanguageDescription.of({
      name: 'INI',
      alias: ['ini', 'cfg', 'conf', 'properties'],
      extensions: ['ini', 'cfg', 'conf', 'properties', 'toml-config', 'gitconfig'],
      filename: /^(\.gitconfig|\.editorconfig)$/,
      async load() {
        const {StreamLanguage} = await import('@codemirror/language');
        const {properties} = await import('@codemirror/legacy-modes/mode/properties');
        return StreamLanguage.define(properties);
      },
    }),
    codemirrorLanguage.LanguageDescription.of({
      name: 'TOML',
      extensions: ['toml'],
      async load() {
        const {StreamLanguage} = await import('@codemirror/language');
        const {toml} = await import('@codemirror/legacy-modes/mode/toml');
        return StreamLanguage.define(toml);
      },
    }),
    codemirrorLanguage.LanguageDescription.of({
      name: 'Shell',
      alias: ['bash', 'sh', 'zsh'],
      extensions: ['sh', 'bash', 'zsh', 'fish'],
      filename: /^(\.bashrc|\.bash_profile|\.zshrc|\.profile|\.envrc|PKGBUILD)$/,
      async load() {
        const {StreamLanguage} = await import('@codemirror/language');
        const {shell} = await import('@codemirror/legacy-modes/mode/shell');
        return StreamLanguage.define(shell);
      },
    }),
    codemirrorLanguage.LanguageDescription.of({
      name: 'Lua',
      extensions: ['lua'],
      async load() {
        const {StreamLanguage} = await import('@codemirror/language');
        const {lua} = await import('@codemirror/legacy-modes/mode/lua');
        return StreamLanguage.define(lua);
      },
    }),
    codemirrorLanguage.LanguageDescription.of({
      name: 'Ruby',
      extensions: ['rb', 'gemspec', 'rake', 'ru'],
      filename: /^(Gemfile|Rakefile|Capfile|Guardfile|Vagrantfile)$/,
      async load() {
        const {StreamLanguage} = await import('@codemirror/language');
        const {ruby} = await import('@codemirror/legacy-modes/mode/ruby');
        return StreamLanguage.define(ruby);
      },
    }),
    codemirrorLanguage.LanguageDescription.of({
      name: 'Dockerfile',
      extensions: ['dockerfile'],
      filename: /^(Dockerfile|Containerfile)(\.[A-Za-z0-9_-]+)?$/,
      async load() {
        const {StreamLanguage} = await import('@codemirror/language');
        const {dockerFile} = await import('@codemirror/legacy-modes/mode/dockerfile');
        return StreamLanguage.define(dockerFile);
      },
    }),
    codemirrorLanguage.LanguageDescription.of({
      name: 'Perl',
      extensions: ['pl', 'pm', 't'],
      async load() {
        const {StreamLanguage} = await import('@codemirror/language');
        const {perl} = await import('@codemirror/legacy-modes/mode/perl');
        return StreamLanguage.define(perl);
      },
    }),
    codemirrorLanguage.LanguageDescription.of({
      name: 'Nginx',
      filename: /(^|\/)nginx(\.[A-Za-z0-9_-]+)?(\.conf)?$/,
      async load() {
        const {StreamLanguage} = await import('@codemirror/language');
        const {nginx} = await import('@codemirror/legacy-modes/mode/nginx');
        return StreamLanguage.define(nginx);
      },
    }),
    codemirrorLanguage.LanguageDescription.of({
      name: 'Diff',
      extensions: ['diff', 'patch'],
      async load() {
        const {StreamLanguage} = await import('@codemirror/language');
        const {diff} = await import('@codemirror/legacy-modes/mode/diff');
        return StreamLanguage.define(diff);
      },
    }),
