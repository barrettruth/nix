#!/bin/sh

waybar_pattern='(^|/| )waybar($| )|\.waybar-wrapped$'

case "$1" in
toggle)
  if pgrep -f "$waybar_pattern" >/dev/null 2>&1; then
    pkill -f "$waybar_pattern"
  else
    waybar >/dev/null 2>&1 &
  fi
  ;;
*)
  printf '%s\n' "usage: waybarctl toggle" >&2
  exit 1
  ;;
esac
